# FreeAI Proxy
Zero-cost AI model routing. Routes every AI request to the cheapest capable model.
## Install  
```bash
pip install freeai-proxy
npx freeai-proxy
curl -fsSL https://freeai-proxy.pages.dev/install.sh | bash
```
